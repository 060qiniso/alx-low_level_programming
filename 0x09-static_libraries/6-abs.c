#include "main.h"
/**
 * _abs - Write a function that computes the absolute value of an integer
 * @n: integer to be checked
 * Return: value of an absolute integer
 */
int _abs(int n)
{
        if (n >= 0)
        {
                return (n);
        }
        return (-n);
}
